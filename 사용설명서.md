# 「내 기록」 사용설명서

## 이 앱의 목적

머리에 담아두지 말고 전부 여기에 던지기 위한 앱. 던지는 순간부터 시스템이 대신 기억하고, 해야 할 일은 완료할 때까지 나를 괴롭히고, 끝난 일도 버리지 않고 보존해서 몇 년 뒤에도 꺼내볼 수 있게 한다.

**원칙: 심플함.** 화면 하나, 입력창 하나. 위에는 요약 타일(괴롭히기), 아래는 보드·달력·표·타임라인(관리). 기능을 늘릴 때도 화면·버튼·개념을 늘리지 않는 방향을 우선한다.

**디자인 원칙 (2026-07-30 스티치 시안 리디자인):** 웹앱이 아니라 **프로그램처럼** — 흰 바탕 + 1px 가는 구분선 + 파란 포인트 하나. **글씨는 작게, 여백은 넉넉히** — 빽빽해서 일하기 싫어지는 화면 금지. 화면 맨 아래 상태줄에 "메모 N건 · 동기화 정상"이 항상 보인다.

**글씨와 쓰는 칸 (2026-08-11 정리):** 글씨는 한 치수 더 작아졌고 크기가 **네 단계**(제목·본문·부가정보·이름표)로 통일됐다 — 곳곳에 조금씩 다른 크기가 섞여 어수선하던 것을 정리한 것. 글꼴도 **Pretendard**로 바꿨다(윈도우 기본 한글 글꼴이 작은 글씨에서 지저분했다). 그리고 **글을 쓰는 곳은 전부 같은 얼굴** — 제목·작업 설명·진행사항 추가 칸이 평소에도 연회색 네모로 보이고, 커서를 놓으면 흰 바탕에 파란 테두리가 켜진다. 예전엔 테두리가 없어서 "여기가 쓰는 칸인지" 눌러봐야 알 수 있었다. 진행 기록의 **[추가] 버튼은 파란색** — 이 줄에서 뭘 누르면 되는지가 색으로 보인다.

---

## 1. 새 메모 — 보드 칸의 + (2026-07-24 재편)

맨 위 던지기 입력창은 없앴다. 대신 **보드의 각 칸(할일·진행중·완료) 머리에 있는 +** 를 누르면 그 칸에 새 메모가 생기고 **오른쪽에 상세 패널이 그대로 열린다**(작성 패널이 따로 있는 게 아니라 상세와 똑같다):

- 맨 위 **제목**을 바로 입력한다(빈 채로 열려 커서가 가 있다).
- **예정 날짜**는 기본이 **오늘** — 안 건드리고 넘어가도 되고, 바꾸려면 그 자리 날짜칸을 눌러 고른다(D-day가 옆에 표시).
- 아래 **작업 설명 · 진행 기록**은 기존 상세와 똑같이 바로 쓸 수 있다.
- 제목·기록을 아무것도 안 쓰고 패널을 닫으면 그 빈 메모는 **자동으로 사라진다**(휴지통에도 안 남음).
- **제목은 이제 상세에서 언제든 바로 고쳐 쓸 수 있다**(예전엔 "정보 수정"으로만).

예: "A업체 계약 26.5.30~27.5.29" / "7/6 이사" / "내일 세금계산서 확인"

자동 인식되는 것:

| 인식 대상 | 예시 |
|---|---|
| 기간 (~) | 26.5.30~27.5.29, 7/3~7/5 |
| 날짜 | 7.6, 7/6, 26.7.6, 7월 6일 |
| 상대 날짜 | 오늘, 내일, 모레, 다음주 |

- 인식된 건 입력 아래 칩으로 표시. 맞으면 Enter로 저장, 틀리면 칩의 ×로 제거.
- **달력에서 새로 만들 땐** PC는 날짜 칸에 마우스를 올리면 우측 상단에 뜨는 **+** 를 누른다 — 그 날짜(예정)로 새 메모가 생기고 오른쪽에 상세가 열린다(보드 +와 같은 흐름). 폰은 날짜를 눌러 아래 목록의 "바로 추가"를 쓴다. 표·타임라인에는 추가 입력이 없다(보드·달력에서 만든다).
- 날짜는 예정 칸으로 빠지고 제목에는 내용만 남는다 ("7/6 이사" → 제목 "이사", 예정 7/6).
- **연도 없이 쓴 날짜가 이미 지난 날이면 내년으로 해석** — 여름에 "1/20 보안교육"을 던지면 내년 1월 20일. (지난 날짜로 적고 싶으면 연도까지 쓰기: "26.1.20")
- **마감형 — "~까지"**: "7/30까지 보고서"처럼 쓰면 그날 하는 일이 아니라 **그날까지 끝낼 일**로 인식. 던진 날부터 계속 보이고, 카드 배지에 **"7.30까지 D-n"** — 어느 날이 마감인지 날짜가 같이 보인다 (2026-07-19). 달력에는 **마감일에만 빨간 조각 하나** (예전엔 시작·중간 조각까지 그려져 한 메모가 여러 개처럼 겹쳐 보였음 — 정리됨).
- **날짜 없이 저장하면 자동으로 오늘 예정** — 바로 "오늘 할 일"에 들어가고, 안 하면 다음날부터 "N일째"로 괴롭힌다.
- **다른 날짜에 넣고 싶으면**: 글에 날짜를 쓰거나("7/20 견적 회신"), 칩 줄의 **"날짜 지정" 버튼**으로 달력에서 고른다 — 고른 날짜가 "예정" 칩으로 붙고, 틀리면 ×. 글에 쓴 날짜보다 버튼으로 고른 날짜가 우선.
- **"보관함에 넣기" 버튼**: 할 일이 아니라 **알아둘 것**(연락처, 모델명, 비밀번호 힌트 등)을 넣는 곳. 날짜 없이 저장되어 오늘·달력에 절대 안 뜨고, 메모탭 검색으로 꺼내보거나 **사이드바 맨 아래 "보관함 n" 버튼**(폰은 상단, 2026-07-22)으로 전체 목록을 훑어본다. 검색 결과에 보라색 "보관" 배지로 구분되어 보인다. 보관함 목록은 휴지통과 같은 구조 — 제목을 누르면 상세가 열리고, 줄마다 **"오늘로" / "날짜 지정"** 버튼으로 할 일로 내보낸다. 목록 위 체크 줄에서 전체 선택·개별 체크를 하면 옆에 "오늘로 n개 / 날짜 지정 n개" 버튼이 나와 여러 개를 한 번에 보낼 수 있다. ⚠️ 비밀번호 원문은 넣지 말 것 — 서버·백업 파일에 평문으로 저장된다. 본인만 아는 힌트만. (주간 기간으로 넣고 싶으면 "7/14~7/18"처럼 기간을 직접 쓰면 된다)

## 2. (제거됨) 업체 인식·이어붙이기

업체라는 개념은 앱에 두지 않는다 (2026-07-15 결정). "라온"이 업체라는 건 내가 아는 것이고, 앱에서는 **검색어**일 뿐 — 검색하면 계약·수리·방문 등 모든 이력이 나오고 그걸로 후속 업무를 진행한다. 같은 건의 기록은 기존 메모를 열어 진행기록에 이어 쓴다.

## 3. 위쪽 줄 — 던지기 + 보기전환 + 검색 (2026-07-24 재편)

화면 맨 위 왼쪽에 **던지기 입력**, 그 오른쪽 위에 **보기전환(보드·달력·표·타임라인)**, 바로 아래 한 줄이 **검색**이다.

- **오늘·마감·만기 타일은 제거했다 (2026-07-24)**. 예전엔 위에 "오늘 n / 마감·만기 n" 타일이 있어 눌러 거르는 시간 필터였는데, 급한 순서는 이미 **보드 정렬(밀림→오늘→D-n)과 D-n 배지 색(가까울수록 따뜻함)**으로 충분히 보여서 없앴다. 필터가 필요하면 **검색**을 쓴다.
- **미루기는 메모를 눌러 여는 패널에서**: **하루 미루기** · **날짜 지정**. 완료는 상세 맨 위 **상태 드롭다운**(PC)·상태 버튼(폰)으로 — 별도 완료 처리 버튼은 2026-07-30 제거. 하루 미루기 = 밀린 것·오늘 것은 내일로, 이미 미래 날짜면 하루 뒤로. 날짜 지정 = 고른 날짜로 이동(달력도 함께). 계약 만기 알림만은 만기일을 건드리지 않고 그 날짜까지 숨김 — 단 **마감·만기가 이미 오늘이거나 지난 메모는 숨길 날이 없으므로 하루 미루기·날짜 지정이 마감(만기) 날짜 자체를 옮긴다** (2026-07-31). 상태 바꾸기는 체크(자동)나 보드 드래그·드롭다운으로.
- **완료 되돌리기**: 완료를 누르면 화면 아래에 "'○○' 완료 — 되돌리기"가 몇 초간 뜬다. (놓쳤으면 완료 열 → 다시 열기)
- 기간 메모에 오늘 날짜의 진행기록 줄이 있으면 보드 카드에 그 줄이 보인다 (예: 오늘의 식단).

**모니터가 달라도 비슷한 크기로 (2026-07-22)**: 회사의 큰 모니터에서는 글씨·칸이 작고 성기게, 집 화면에서는 크게 보이던 문제 때문에 **화면이 넓을수록 앱 전체 배율이 조금씩 커진다** (1600px 넘으면 5%, 1900px 10%, 2300px 15%). 어느 컴퓨터에서 열어도 비슷한 크기로 보인다. 처음엔 30~45%까지 키웠다가 "그냥 확대해놓은 것 같고 밸런스가 안 맞는다"는 피드백으로 지금 수준까지 낮췄다 (2026-07-22). 그래도 더 크게/작게 보고 싶으면 브라우저 확대(Ctrl + `+`/`-`)를 쓰면 되고, 폰은 배율이 그대로다.

**모든 보기가 같은 규칙 (2026-08-11)**: 평소에는 **화면을 넓게** 쓰고, 항목을 눌러 상세를 열면 **왼쪽으로 살짝 줄어들며** 오른쪽에 상세가 붙는다. 표·타임라인·루틴이 그렇게 동작했는데 그게 낫다고 해서 **보드와 달력도 같게** 맞췄다(예전엔 보드·달력이 늘 화면 절반에 고정이었다). 달력은 날짜를 눌러 오른쪽에 목록이 뜰 때 줄어든다 — 평소엔 칸이 넓어 제목이 훨씬 덜 잘린다. 평소 폭은 **메모·루틴과 같은 선에서 멈춘다**(달력만 화면 끝까지 뻗으면 오른쪽 끝이 혼자 어긋나 보인다). 목록이 뜰 때만 화면 끝까지 넓어져 그 자리를 만든다.

**왼쪽 메뉴 (2026-08-11 정리)**: 위에는 매일 쓰는 **메모·루틴**만 두고, 서랍(보류함·보관함·휴지통)과 도구(백업·가져오기)는 **맨 아래 아이콘 한 줄**로 내렸다. 건수는 아이콘 위 파란 동그라미. 계정(이메일)도 맨 아래 로그아웃 옆으로 옮겼다 — 누르면 진단 결과가 뜬다.

PC 레이아웃 (2026-07-30 리디자인): 상세 패널이 열리면 화면이 좌우로 나뉜다 — 분할선은 **왼쪽 60 : 오른쪽 40** 지점 — 달력 칸의 글자가 너무 빨리 잘려서, 오른쪽 목록에서 폭을 떼어 달력·보드 쪽에 줬다 (2026-08-11, 그전에는 정중앙 50:50). 달력 칸에 보이는 글자가 약 1.3배로 늘어난다. 상세는 떠 있는 카드가 아니라 **분할선에 붙은 전체 높이 패널**이다. 상단바는 모든 보기 공통 — 왼쪽 밑줄 탭(보드/달력/표/타임라인) + 오른쪽 검색. 사이드바는 메모·보류함·보관함·휴지통·백업 메뉴(아이콘 + 건수)와 맨 아래 로그아웃.

의도: "A업체 계약 26.5.30~27.5.29"라고 메모만 해두면 내년 봄에 만기 타일에 1이 켜진다. 내가 기억하는 게 아니라 시스템이 기억한다.

## 4. 진행 기록 — 과정을 남기는 곳

메모를 누르면 상세가 열린다 (팝업 없음). **PC는 오른쪽 패널**로 붙어 달력·목록을 보면서 동시에 기록하고, **폰은 누른 줄이 그 자리에서 상세로 바뀐다** (2026-07-20 — 예전엔 줄 아래 새 창이 붙어 제목이 두 번 보였는데, 이제 줄 자체가 펼쳐진다). 접을 때는 상세 오른쪽 위 **"접기"** 버튼.

**폰의 상태 이동 (2026-07-18)**: PC는 보드에서 카드를 드래그해 할일↔진행중↔완료를 옮기지만 폰은 드래그가 안 되므로, 폰 상세 맨 위에 **상태: [할일] [진행중] [완료]** 버튼이 있다 — 누르면 보드 열이 바로 바뀐다. (폰에서는 이 버튼이 완료 처리·다시 열기 역할까지 하므로 별도 완료 버튼은 없다)

**폰 화면 정리 (2026-08-05)**: 7월 말 리디자인이 PC 위주였어서 폰에 남아 있던 것들을 한 번에 정리했다.
- **맨 위 줄**: 예전엔 이메일·보류함·보관함·휴지통·백업·로그아웃이 작은 글자로 두 줄을 차지했다. 이제 **PC 왼쪽 메뉴와 똑같은 그림(아이콘)이 한 줄**이고, 건수는 아이콘 위 파란 동그라미로 붙는다. **지금 보고 있는 곳(보류함 등)은 아이콘이 파랗게 눌린 채**로 보인다. 제목 옆 **작은 점이 동기화 표시** — 초록이면 정상, 빨강이면 안 맞춰진 것이고, **누르면 진단 결과**(계정·서버에 있는 건수·앱 버전)가 뜬다. 예전 이메일 글자 버튼 자리다.
- **손가락 크기**: 누르는 것은 전부 손가락에 맞게 키웠다(최소 40px). 글을 쓰는 칸은 16px로 올려서 **커서를 놓을 때 아이폰이 화면을 확대해버리던 것**도 없앴다.
- **메모 상세**: 아이콘만 8개 있던 줄에 **이름을 같이** 넣었다(미루기·날짜·마감·보류·보관·수정·삭제) — 폰은 마우스를 올릴 수 없어 그림만으론 알 수가 없었다. **진행기록 한 줄을 지우는 ×** 도 항상 보인다(예전엔 마우스를 올려야 나와서 폰에선 아예 못 지웠다). 진행사항 쓰는 칸은 **글칸이 윗줄 전체**, 날짜·추가 버튼이 아랫줄로 내려가 쓰기 편해졌다.
- **달력**: 기간 메모의 **이어진 띠도 색 막대로** 바뀌었다 — 7/31에 띠가 생기면서 폰 칸에 글자가 그대로 나와 칸이 터졌었다. 달력 아래 날짜 목록은 선 하나로 구분된다. **공휴일 이름도 폰 칸에는 안 넣는다 (2026-08-07)** — 칸이 좁아 "대체공휴일"이 "대…"로 잘려 뜻이 없는 데다, 그 한 줄 때문에 **그 칸만 아래로 밀려 이어진 띠가 어긋나 보였다**. 폰에서는 **빨간 날짜 숫자**가 공휴일 표시이고, 이름은 날짜를 누르면 아래 목록 제목에 "26.08.17 (월) 광복절 대체공휴일"로 나온다. (PC 달력 칸에는 이름이 그대로 보인다)
- **보관함·보류함·휴지통 목록**: 제목이 "엘리베이…"로 잘리던 줄에서 **버튼을 아랫줄로 내려** 제목이 다 보인다.
- **검색어 지우기 (2026-08-07)**: 검색칸에 글자를 치면 **오른쪽 끝에 ×** 가 생긴다. 한 번 누르면 검색어가 통째로 지워지고, 커서는 칸에 남아 있어 폰 키보드가 닫히지 않고 바로 다시 칠 수 있다. (PC는 검색칸에서 **Esc** 로도 지워진다)

**폰의 보드 (2026-07-19)**: PC처럼 **할일|진행중 두 열이 나란히** 보인다 — 뭐가 밀려 있고 뭐가 굴러가는 중인지 한 화면에서 파악. **완료는 아래 "완료 N건 펼치기" 버튼** 뒤에 접혀 있다. 카드를 누르면 상세가 그 줄 아래 **전체 폭**으로 펼쳐진다.

**상세를 닫는 방법 (PC, 2026-07-22 / 손잡이 2026-08-04)**: 오른쪽 패널은 **화면 빈 곳을 클릭**하거나 **Esc**를 누르면 오른쪽으로 쓱 빠져나가며 닫힌다. ×를 굳이 찾지 않아도 된다. 여기에 더해 패널 **좌측 상단에 접기 버튼(»)** 이 있다 — 누르면 패널이 오른쪽으로 접히며 목록으로 돌아간다. (2026-08-11에 분할선 한가운데 세로 손잡이에서 패널 모서리로 옮겼다 — 화면 중앙에 떠 있는 것보다 모서리에 붙어 있는 편이 찾기 쉽다) 단, **다른 메모(카드·줄·달력 항목)를 클릭하면 닫히지 않고 그 메모로 바뀐다**. 글을 쓰는 중(제목 수정·작업 설명 등)에 누른 Esc는 그 입력의 취소로만 쓰이고 패널은 열려 있다. 폰의 인라인 상세는 그대로 — "접기" 버튼으로 닫는다.

**작업 설명 (2026-07-22 추가)**: 상세 안, 진행기록 바로 위에 **"작업 설명"** 칸이 있다. 진행기록이 "언제 무엇을 했는지"의 시간순 줄이라면, 작업 설명은 **"이 일이 무엇인지"** 를 적어두는 고정 칸이다 — 배경·목적·담당자·참고할 내용. 저장 버튼이 없고 **자동 저장**된다(타이핑을 멈추면 곧, 다른 곳을 누르거나 패널을 닫을 때도). 검색에도 걸린다 — 설명에 "라온"이라고 적어두면 "라온" 검색으로 그 메모가 나온다. (노션 세부정보의 '작업 설명'을 모티브로)

**잘못 고쳤을 때 되돌리기 (2026-08-05)**: 작업 설명·제목은 자동 저장이라 고치는 순간 원래 내용이 덮인다. 그래서 **Esc 한 번이면 그 칸에 커서를 놓았을 때의 내용으로 통째로 되돌아간다** — 이미 자동 저장됐어도 되돌린 내용으로 다시 저장되니 안심해도 된다. 되돌릴 게 없을 때 누른 Esc는 그냥 칸에서 빠져나오고(그 다음 Esc는 평소대로 패널 닫기). 되돌리기는 **한 칸 단위, 커서를 놓은 시점 기준**이다 — 다른 곳을 눌렀다가 다시 들어오면 그때부터 새로 기억한다.

- 한 줄씩 추가하면 날짜와 함께 쌓임
- 줄마다 체크박스 — 그 자리에서 하나씩 완료 체크, 진행률(체크 1/3)이 목록에도 표시
- 상태 자동 전환은 **체크 기준**: 줄만 적어둔 건(계획) 할일 그대로, 체크박스를 하나라도 체크하면(착수) 진행중
- 수정이 아니라 누적 — 지난 과정이 지워지지 않는 게 핵심
- 자동 로그 없음. 기록엔 내가 쓴 내용만
- 잘못 적었을 때: 줄의 글자를 누르면 그 자리에서 날짜·내용 수정 (Enter 저장, Esc 취소). 줄에 마우스를 올리면 나오는 ×로 삭제
- **파일 첨부 (2026-07-31 부활)** — 상세 맨 아래 "파일" 칸. 올리는 길 셋: **[+ 올리기]** 버튼 · **끌어다 놓기** · **캡처 붙여넣기(Ctrl+V)** — 캡처는 "캡처 MMDD-HHmm" 이름으로 저장된다. 이미지는 **썸네일 미리보기**가 보이고 누르면 새 탭에서 크게, **다운로드**는 원본 이름 그대로 내려받는다. 25MB 이하, 실물은 클라우드 저장소(Supabase Storage)에 있어 회사·집·폰 어디서든 열린다. (2026-07-16 안 쓰여서 제거했다가 청약 서류 등 필요해져 부활)

## 5. 달력 — 모든 것이 날짜로 연결 (메모탭의 '달력' 보기)

2026-07-15부터 달력은 별도 탭이 아니라 **메모탭 상단 토글(보드/달력/표/타임라인)의 두 번째 보기**다. 검색이 달력에도 적용된다 — "라온"을 검색하면 라온 관련 일정만 있는 달력이 된다. 검색 중에는 달력 위에 노란 안내줄이 떠서 "걸러진 상태"임을 알려준다.

모든 메모가 자기 날짜에 나타난다:

- 날짜(예정·마감) 있는 메모 → 그 날짜
- 기간(~) 메모 → 시작일부터 만기일까지 전부 (시작/만기 표시)

규칙: 메모는 자기 날짜에만 표시되고, 진행 기록(체크리스트)은 메모 안에서만 산다. 7/6 "이사"의 체크리스트는 7/6 메모를 열면 있다 — 적은 날짜에 흩어지지 않는다.

**예외 — 기간 메모의 날짜별 기록**: 기간 메모의 진행기록에 그 날짜 줄이 있으면 달력 칸에 제목 대신 그 줄이 보인다. 주간 식당 메뉴에 딱 좋다: "식당 메뉴"를 이번 주 기간으로 저장하고 진행기록에 날짜별로 "월: 제육볶음…" 5줄을 넣으면, 월요일 칸엔 월요일 메뉴가 그대로 보인다. 오늘 화면에도 오늘 줄이 같이 표시된다.

**기간 메모의 달력 표시 (2026-07-31 개편 — 이어진 띠)**: 마감형이 아닌 기간 메모(31일 이하)는 시작~끝까지 **칸 경계를 넘어 쭉 이어진 띠**로 그려진다 (구글 캘린더식 — 청약 서류접수 기간 같은 것). 제목은 시작날과 매주 첫 칸에 보이고, 그 날짜의 진행기록 줄이 있으면 그 내용이 우선(주간 식단은 그대로). 진행중이면 초록 띠, 완료는 회색 취소선 띠. **띠는 항상 칸 상단 고정** — 이어져 보이려면 모든 칸에서 같은 높이여야 해서, 드래그 순서 변경은 그 아래 당일 칩들끼리만 적용된다. 띠를 끌면 시작·끝 칸은 그쪽 날짜만, 중간은 기간 전체가 평행이동. **한 달 넘는 기간은 칸에 안 그려지고**(도배 방지) 날짜를 누르면 그날 목록에 "기간 중 (n일차)"로 나온다. 마감형은 기간 내내가 아니라 **마감일에 빨간 조각 하나만**.

**칸에는 항상 "딱 떨어지는 줄"만 (2026-08-11)**: 칸 높이는 화면 세로 ÷ 그 달의 주 수라 예전엔 마지막 줄이 반쯤 걸쳐 보였다. 이제 칩 목록이 **한 줄 높이의 배수로만 열려서**, 3줄이면 딱 3줄이고 자투리는 그냥 비운다. 줄 높이도 조여서(글자 크기는 그대로, 줄간격·여백만) 같은 칸에 **한 줄 정도 더** 들어간다. 몇 줄이 보이는지는 모니터 세로 길이와 그 달의 주 수(5주냐 6주냐)가 정하고, 안 보이는 건 칸 오른쪽 아래 **"+N"** 과 날짜를 눌렀을 때의 목록에 있다. **완료된 기간 띠는 이름 없이 얇은 회색 막대**로 눕혔다 — 한 주에 완료된 기간이 하나만 있어도 그 주 일곱 칸이 모두 자리를 비워야 해서, 끝난 일이 남은 일의 줄을 잡아먹던 것을 3분의 1로 줄였다(이름은 막대에 마우스를 올리면 뜬다).

**달력 칸에는 제목만 (2026-08-11)**: 칸의 조각 앞에 붙던 **"예정 · 마감" 글머리를 뺐다** — 세 글자가 제목을 잡아먹는데, 종류는 이미 색이 말한다(**빨강=마감 · 파랑=예정 · 연보라=기간**). 글자로 된 종류 표시는 날짜를 눌렀을 때 오른쪽 목록의 배지와 상세에 그대로 있고, **칸의 조각에 마우스를 올리면 잘린 제목 전체가 말풍선**으로 뜬다. 칸 여백도 함께 줄여서, 분할선 옮긴 것까지 합치면 칸에 보이는 글자가 예전의 두 배쯤 된다.

달력에서 **오늘은 날짜 숫자의 파란 동그라미**, **클릭해서 선택한 날은 파란 테두리** — 둘이 같이 보여도 다중선택이 아니다. **완료된 메모의 칩은 회색·취소선**, **진행중인 메모의 칩은 보드의 진행중과 같은 초록**(2026-07-26) — 달력만 훑어도 굴러가는 건이 보인다. 단 마감(빨강)은 급한 표시가 우선이라 진행중이어도 빨강 그대로다. 항목 위에 마우스를 올리면 그 항목만 테두리가 켜지고(누르면 상세), 빈 곳에 올리면 칸이 살짝 어두워진다(누르면 그날 전체 목록).

날짜 클릭 → 그날 목록이 뜨고, 그 자리에서 바로 새 메모 작성 가능. **PC는 목록이 달력 오른쪽 칸에 나오고, 그 목록에서 한 줄을 누르면 상세가 목록 맨 위에 펼쳐진다 (2026-07-23)** — 화면 이동 없이 옆에서 바로 읽고 고칠 수 있다(폰은 아래에 펼쳐짐, 아래 참고). **클릭한 날짜가 무조건 예정일이 된다** — 글에 "7/20 메일참고"처럼 날짜가 섞여 있어도 파싱하지 않는다 (글 속 날짜가 마감을 뺏어 엉뚱한 날짜에 등록되던 문제 수정, 2026-07-21). 한 칸에 4개 초과는 +n으로 접힘.

**PC 달력 (2026-07-31 보드 기준 통일)**: 왼쪽 절반 = 달력 — **화면 높이에 딱 맞아 페이지 스크롤이 없고**, 격자는 스프레드시트식 헤어라인. 오른쪽 절반: ① 날짜를 누르면 그날 **목록**(+빠른 추가), ② 항목을 누르면 **보드·표와 똑같은 도킹 상세 패널**이 우측 절반을 덮는다 — 상태 드롭다운·아이콘 줄·여백 전부 동일. ③ **×·빈 곳 클릭·Esc**로 상세 → 목록 → 닫힘 순으로 한 단계씩 물러나고, 패널이 닫히면 밑의 목록이 그대로 드러난다. 이때 **오른쪽 칸 안쪽은 목록이 짧아 밑이 비어 보여도 "빈 곳"이 아니다** — 상세 패널과 마찬가지로 눌러도 닫히지 않는다 (2026-08-04). 좌우를 가르는 **세로 분할선**은 상세를 닫아도 늘 보인다 — 화면이 두 칸으로 나뉜 프로그램의 틀 (2026-08-04). 상세의 예정 날짜 옆 **"기간으로"** 를 누르면 기간(시작~끝) 일정으로 바로 바꿀 수 있다. (폰은 아래 설명대로 달력 아래에 목록.)

**폰에서는 달력이 다르게 보인다 (2026-07-19 확정)**: 칸이 좁아 글자가 안 읽히므로 칸에는 **색 막대만** 표시된다(어느 날에 뭐가 몇 개 있는지 한눈에). 달력 바로 아래에는 **선택한 날짜 하나의 목록만** 붙는다 — 열 때부터 오늘이 선택돼 있고, **다른 날짜를 누르면 화면 이동 없이 그 자리에서 내용만 바뀐다** (아이폰 기본 달력과 같은 조작감). 한때 달 전체를 아래에 나열하고 누르면 스크롤 이동하는 방식을 썼으나 조작감이 나빠서 뺐다.

**드래그로 우선순위**: 날짜를 눌러 펼쳐진 목록에서 항목을 끌어 순서를 바꾸면 그날의 우선순위가 된다. 이 순서는 저장되고, 달력 칸 안의 칩 순서와 오늘 화면의 "오늘 할 일" 순서에도 그대로 반영된다. 단 **상태가 먼저다 (2026-07-22)** — 진행중 → 시작전 → 완료 순으로 자동 정렬되고, 드래그 순서는 같은 상태끼리 안에서만 적용된다. **기간 띠도 같은 규칙 (2026-08-11)** — 진행중인 기간이 칸 위, 끝난 기간의 띠는 칸 맨 아래에 붙는다(전에는 완료된 띠가 칸 맨 위에 남아 있어 남은 일이 그 아래로 밀렸다).

**드래그로 이동**: 달력의 메모를 끌어서 다른 날짜에 놓으면 날짜가 바뀐다.
- 예정 메모 → 예정일이 놓은 날짜로
- 날짜 없는 메모 → 놓은 날짜가 예정일이 됨
- 기간 메모 → 시작/만기 조각을 끌면 그쪽 끝만, 중간 조각을 끌면 기간 전체가 이동
- (드래그는 PC 전용. **날짜를 옮기려면 메모를 열어 상세의 "날짜 지정"** 을 쓴다 — 목록 줄의 "날짜 이동" 버튼은 2026-07-24 제거. 드래그·상세로 충분해서.)

의도: 달력만 넘겨봐도 하루하루가 재구성되는 것.

## 5-1. 루틴 — 매달·해마다 도는 일 (2026-08-11 신설)

왼쪽 메뉴의 **루틴**. 공과금·월세·유지보수료·정기방문 보고처럼 **매달 같은 이름으로 돌아오는 일**을 **항목 × 12개월 격자**로 본다. 엑셀 「월간체크리스트」에서 O를 치던 그 표를 앱으로 옮긴 것이고, 메뉴 옆 숫자는 **이번 달에 아직 안 끝난 건수**다.

**두 겹으로 되어 있다 — 이게 핵심이다.**

- **행(항목) = 반복 규칙**: 이름, 묶음(공과금·구미 / 안전·소방 …), 그리고 **매달 똑같이 붙는 설명**("검침 16일~익월 15일 · 회계전표 · 영남에너지서비스"). 엑셀의 비고 열이 여기 온다. 한 번 써두면 계속 따라다닌다.
- **칸 = 그 달의 메모**: 항목 **이름을 누르면** 고른 달의 회차가 평소 쓰던 상세로 열린다. **그 달만의 특이사항**("502호 고지서 누락 — 재발행 요청")·파일 첨부는 전부 여기 쌓인다. 격자에는 노란 점으로 "이 달엔 뭔가 있었다"만 표시된다. 몇 년 뒤 검색하면 그대로 나온다.

**쓰는 법**

- **칸을 누르면 그 달 완료**. 회차는 그냥 메모라서 **달력·오늘 화면에도 같이 뜨고**, 거기서 완료해도 격자가 따라 켜진다.
- **완료를 푸는 건 두 번 눌러야 한다 (2026-08-14)** — 해놓은 게 실수로 지워지는 걸 막는다. 완료된 칸을 한 번 누르면 그 칸이 **주황 테두리에 ↺** 로 바뀌며 물어보고, **한 번 더 누르면** 풀린다. 4초 안에 안 누르면 저절로 없던 일이 된다(팝업은 안 뜬다 — 이 앱은 팝업을 안 쓴다). **완료 켜는 건 그대로 한 번** — 매달 수십 번 누르는 자리라 두 번 누르게 하면 성가시다. 잘못 켰으면 화면 아래 **"○○ 8월 — 완료 · 되돌리기"** 가 6초간 떠 있으니 그걸 누르면 된다.
- **고른 달의 안 한 칸은 주황으로 물든다 (2026-08-14)** — 왼쪽 위 "8월 27건 남음" 배지와 같은 색이라, **그 배지 숫자 = 주황 칸 수**다. 월 머리(1~12)를 눌러 달을 바꾸면 색도 그 달로 옮겨간다. 지난 달의 안 한 칸까지 물들이면 격자가 온통 주황이 되어 아무것도 안 보이므로 **고른 달만** 물든다.
- **월 머리(1~12)를 누르면** 그 달이 "고른 달"이 된다 — 이름을 눌렀을 때 열리는 회차가 그 달이다.
- **항목 추가**는 묶음 머리의 **+ 항목**, 고치기·중단·삭제는 줄 오른쪽 **⋯**.
- **끝난 루틴은 "중단"** (⋯ → 중단). 지우는 게 아니라 끝내는 것이라 **지난 처리 이력이 그대로 남는다** — 엑셀에서 행을 지우면 이력까지 사라지던 것과 다르다. 중단한 항목은 격자 맨 아래 **"▸ 중단 N건"** 에 접혀 있고, 펼치면 그때까지의 체크가 다 보인다. 회차 메모도 메모로 남아 몇 년 뒤 검색된다. (담당이 바뀐 항목은 이렇게 둔다. 되살리려면 ⋯ → 중단 해제)
- **삭제**(⋯ → 삭제)는 목록에서 그 줄만 없앤다 — 이미 만들어진 회차 메모는 메모로 남는다.
- **날짜가 고정인 일 vs 매번 잡는 일 (2026-08-13 신설)** — 공과금·월세는 매월 같은 날이지만, **업체 방문·정기점검은 통화해서 날짜를 잡는 일**이다. 그런데 회차가 자동으로 생기면 달력에 날짜가 찍히고, 나중에 보면 **"이게 내가 잡은 약속인지, 앱이 자리만 잡아둔 날인지" 구분이 안 됐다**. 그래서 루틴마다 **날짜: [고정 / 매번 잡음]** 을 둔다.
  - **매번 잡음**으로 두면 그 회차는 **"가예정"** 으로 태어난다 — 달력·날짜 목록·보드에서 **점선 + 흐린 글씨**(폰 달력에서는 색 막대가 옅게)로 보인다. **흐린 건 아직 안 잡은 것, 진한 건 내가 정한 약속**이다.
  - **날짜를 옮기거나**(달력에서 끌기·날짜 지정·하루 미루기·정보 수정) **진행 기록을 한 줄 쓰면** 저절로 확정되어 진해진다. 따로 누를 게 없다.
  - 자동 날짜가 마침 맞으면 상세의 **[이 날로 확정]** 한 번. (상세의 이름표도 "예정 (아직 안 잡음)"으로 뜬다)
  - **기존 루틴은 전부 "고정"** 이다 — 쓰던 화면이 갑자기 흐려지지 않는다. 유동적인 것만 골라서 바꾸면 된다.
  - 묶음 이름 옆 **[날짜]** → **[방식만 적용]** 이면 그 묶음 전체가 한 번에 바뀐다(예: 「유지보수」 전부를 매번 잡음으로). **예정일은 안 건드린다** — 항목마다 날짜가 다르기 때문이다. 완료된 회차와 이미 내가 잡은 날짜도 그대로 둔다.
  - 한 항목만 바꾸려면 그 줄의 **⋯ → 수정 → 날짜**.
- **예정일을 묶음 통째로** 바꿀 수도 있다 — 묶음 이름 옆 **[날짜]** → 아랫줄 **예정일 통일**에 날짜를 넣고 **[전부 이 날로]**. ⚠️ **항목마다 예정일이 다르면 그 날짜들이 전부 덮인다** — 그래서 빨간 글씨이고, 누르면 그 자리에서 [정말 전부 바꿉니다]로 바뀌어 한 번 더 눌러야 적용된다(팝업 아님). 처음 엑셀에서 가져와 날짜가 전부 같을 때 쓰는 것이고, 평소에는 개별(⋯ → 수정)로 고친다. 34건을 하나씩 고칠 일이 없다. 완료된 회차는 그대로 두고 아직 안 한 회차의 날짜만 같이 옮긴다. (엑셀에 **`예정일` 열**을 만들어 두면 가져올 때 그대로 따라간다)
- **예정일을 고치면 이번 달 회차도 같이 옮겨간다 (2026-08-13 수정)** — 예전에는 ⋯ → 수정에서 예정일을 5일→14일로 바꿔도 **달력의 이번 달 회차는 5일에 그대로 남아 있었다**(묶음 일괄로 바꿀 때만 옮겨졌다). 이제 개별로 고쳐도 아직 안 끝난 회차가 그 날짜로 따라온다. 완료된 회차는 그대로 둔다 — 지난 일은 지난 일이다.
- **묶음 이름을 자유롭게 고친다 (2026-08-21 신설)** — 묶음 이름("공과금·구미" 등)을 누르면 그 자리에서 입력칸이 되고, 새 이름을 적고 Enter(또는 다른 곳을 누름)하면 **그 묶음의 모든 항목이 한 번에** 새 이름이 된다. 다른 묶음과 같은 이름으로 고치면 그 묶음으로 합쳐진다.
- **묶음을 새로 만들거나 통째로 지운다 (2026-08-22 신설)** — 표 위쪽 **[+ 새 묶음]** 에 이름을 적고 만들기를 누르면 그 묶음의 첫 항목 만들기로 바로 이어진다(항목 이름까지 적어야 묶음이 실제로 남는다 — 비워두고 나가면 없던 일이 된다). 지우려면 그 묶음의 **[날짜]** 를 열고 맨 아래 **[묶음 지우기]** — 예정일 통일과 같은 방식으로 한 번 더 눌러야 지워진다. **기록이 남은 회차(완료·메모·파일)는 지워도 메모로 그대로 남고**, 아무것도 안 한 빈 회차만 같이 없어진다. 중단한 항목은 손대지 않는다.
- **위치를 드래그로 옮긴다 (2026-08-21 신설)** — 항목 줄을 눌러서 끌면 그 자리로 옮겨진다. **다른 묶음의 항목 위에 놓으면 그 묶음으로도 옮겨간다**(묶음 이름은 항목 수정 칸에서 다른 이름을 적어도 옮겨진다 — 이때 지금 있는 묶음 이름이 아래로 뜬다). **묶음 머리 위에 놓으면 그 묶음 맨 끝으로** 들어간다. 묶음 자체를 통째로 옮기려면 이름 왼쪽의 **⠿ 손잡이**를 끌어 다른 묶음의 앞/뒤에 놓는다 — 안의 항목 순서는 그대로 딸려간다. (끝난 루틴은 접혀 있어 순서를 안 바꾼다)
- **메모를 루틴으로 (2026-08-21 신설)** — 매달 메모에 적어 오던 점검 기록이 있다면, 그 메모 상세의 **예정 줄에 있는 [루틴으로]** 를 누르면(한 번 더 눌러 확인) 매달 도는 일이 된다. **그 메모가 그 달 기록이 되고**(적어둔 진행 기록·파일 그대로), 다음 달부터 루틴 격자에 자동으로 나온다. 예정일은 그 메모 날짜, 시작 달도 그 메모의 달이라 **그 앞의 달들은 "해당 없음"(–)** 으로 남는다. 다음 달 회차는 **다른 루틴과 똑같이(흐리지 않게) 뜬다** — 날짜가 매달 조금씩 달라도 그 근처에서 처리하고 실제 한 날로 옮기면 된다. 묶음은 처음에 **기타**로 들어가니 ⋯ → 수정에서 바꾸면 된다. 이미 루틴 회차인 메모에는 이 버튼이 안 보이고, 대신 **↻ 루틴 · 매달 N일** 칩이 보인다.
- **날짜 방식을 바꾸면 이번 달 회차도 따라간다 (2026-08-22 수정)** — ⋯ → 수정에서 **날짜: 매번 잡음**으로 바꿔도 **이미 만들어진 그 달 회차는 진하게(확정) 남아 있었다** — 묶음 일괄로 바꿀 때만 따라왔다. 이제 개별로 바꿔도 아직 안 끝난 회차의 가예정 표시가 같이 바뀐다. 단 **내가 손 댄 회차는 안 건드린다**: 기록을 한 줄이라도 썼거나, [이 날로 확정]을 눌렀거나, 자동 날짜에서 다른 날로 옮겨둔 회차는 이미 내가 잡은 약속이라 진한 채로 둔다. (앱을 열 때 지금까지 어긋나 있던 회차도 한 번 맞춰준다)
- **실제로 한 날을 예정일로 삼는다 (2026-08-20 신설)** — 루틴 예정일이 17일인데 실제로는 18일에 하고 그 날로 옮겨 완료했다면, 회차 상세의 예정 칸 아래에 **"루틴 예정일은 17일 — 앞으로도 매달 18일에 할까요?"** 가 뜬다. **[매달 18일로]** 를 누르면 규칙이 18일이 되어 **다음 달부터 18일로 온다**(아직 안 끝난 회차도 같이 옮겨간다). 휴일·출장으로 그 달만 밀린 것이면 **[이번만]** — 규칙은 그대로 두고 다시 안 묻는다. 아무것도 안 눌러도 그 달만의 일로 남는다. **매번 날짜를 잡는 일(가예정)은 묻지 않는다** — 원래 달마다 다른 날이다.
  **8월치는 한 번에 맞춰뒀다** — 8월에 날짜를 옮겨 완료해 둔 항목들은 앱을 열 때 그 날짜가 예정일로 자동 반영됐다(화면 위에 몇 건인지 안내가 한 번 뜬다). 9월부터는 위처럼 그때그때 물어본다.
- **이름을 고치면 회차 제목도 따라간다 (2026-08-14 수정)** — 격자에 보이는 건 **규칙 이름**이고, 달력·오늘·검색에 뜨는 건 **회차 제목**("○○ — 8월분")이다. 그래서 회차 상세에서 제목만 고치면 격자는 그대로였다. 이제 **⋯ → 수정에서 이름을 고치면 이미 만들어진 회차 제목도 같이 바뀐다**(지난 달·완료한 것 포함 — 이름은 딱지일 뿐 언제 했는지는 그대로다). 단 **그 달만 따로 적어둔 제목은 안 건드린다** — 자동으로 지어진 "○○ — 8월분" 그대로인 것만 바꾼다.
  **반대쪽도 된다** — 회차 상세에서 제목의 **앞 이름만 고치면**("○○ — 8월분" 모양은 그대로 두고) 규칙 이름과 다른 달 회차까지 같이 바뀐다. 어느 쪽에서 고쳐도 결과가 같다. 다만 **"○○ — 8월분 재발행"처럼 그 달 얘기를 덧붙이면 그 달 하나만** 바뀐다 — 그건 이름이 아니라 그 달의 메모라고 본다.
- **예정일은 31일까지 넣을 수 있다 (2026-08-14 수정)** — 예전엔 28일까지만 저장돼서 30·31을 넣으면 조용히 28로 잘렸다(납기 말일 같은 항목을 제대로 못 넣었다). 이제 **29·30·31을 넣으면 그 날이 없는 달은 자동으로 말일**로 간다 — 31일로 해두면 2월은 28일(윤년 29일), 4·6·9·11월은 30일. 29~31을 넣으면 칸 옆에 "그 날이 없는 달은 말일로"라고 알려준다.
- **폰에서 순서 바꾸기 ▲▼ (2026-08-26)** — 달력 아래 날짜 목록에서 줄을 끌어 순서를 바꾸는 건 PC에서만 됐다(폰은 드래그가 안 된다). 이제 폰에서는 줄마다 **▲▼ 버튼**이 붙어 한 칸씩 올리고 내린다. 바꾼 순서는 그대로 저장된다. **같은 상태끼리만 움직인다** — 목록이 진행중 → 할일 → 완료 순으로 먼저 갈리기 때문에, 그 경계에서는 화살표가 흐리게 꺼진다(눌러도 어차피 제자리로 돌아온다). PC는 예전처럼 끌어서 옮기고 ▲▼는 안 보인다.
- **년월을 눌러 한 번에 이동 (2026-08-13)** — 달력 위의 **"2026년 8월"을 누르면** 그 아래에 년·월 고르는 판이 펼쳐진다. 연도는 ‹ › 로, 달은 1~12월 중 눌러서 고른다. ‹ › 를 여러 번 누를 필요 없이 몇 달·몇 년 떨어진 달로 바로 간다. (빈 곳을 누르거나 Esc로 닫힌다)
- **앞으로 올 달에도 루틴이 달력에 보인다 (2026-08-13 신설)** — 달력에서 다음 달·그다음 달로 넘기면 그 달에 해당하는 루틴이 **"예정 자리"로 살짝 옅게** 뜬다. 매월 뭐가 돌아오는지 미리 보인다는 뜻이다. **아직 진짜 회차가 만들어진 건 아니고**, 그 칩을 누르는 순간 그 달 회차가 생기면서 상세가 열린다. 그래서 **오늘 화면·보드는 깨끗하게 유지된다** — 안 한 달이 미리 쌓이지 않는다. (검색 중일 때는 안 그린다 — 검색은 있는 것만 보는 자리다. 중단한 항목과 그 달에 해당 없는 주기도 안 뜬다. 매번 잡는 일은 여기서도 점선·흐리게)
- **지운 회차는 다시 안 생긴다 (2026-08-19 수정)** — 달력에서 그 달 회차를 지워도 다음에 앱을 열면 되살아났다. "만든 적이 있나"를 **보이는 것만 보고** 판정해서, 휴지통에 넣은 건 없는 것으로 치고 다시 만들었기 때문이다. 이제 지운 건 지운 것으로 둔다. (그 달을 다시 만들고 싶으면 격자에서 그 달 칸을 누르거나, 달력의 예정 자리를 누르면 된다)
- **이름 없는 줄이 만들던 "— 8월분" (2026-08-19 수정)** — 눈에 안 보이는 글자(엑셀에서 딸려온 제로폭 공백 등)만 남은 이름은 앱이 "이름 있음"으로 봐서, 이름 없는 회차가 계속 만들어졌다. 이제 그런 것도 **빈 이름으로 보고** 회차를 안 만든다(그 줄 자체도 앱을 열 때 자동으로 치워진다).
- **다음 달 회차는 그 달이 되어야 자동으로 생긴다** — 9월분이 지금 오늘 화면·달력에 안 뜨는 건 의도한 것이다. 안 한 달이 미리 쌓여 화면을 덮지 않게. 미리 잡아두고 싶으면 격자에서 **9월 머리를 누른 뒤 항목 이름을 누르면** 그 자리에서 9월 회차가 생긴다.
- **어디서 완료하든 같이 반영된다** — 회차는 그냥 메모라서, 오늘 화면·달력·보드에서 완료해도 격자에 ✓가 켜지고, 격자에서 눌러도 그쪽에서 사라진다. 하나의 같은 기록이다.
- **왼쪽 위 "○월 N건 남음"은 고른 달 기준**이다. 월 머리(1~12)를 누르면 그 달의 남은 건수로 바뀌고, 다 했으면 초록색 "다 끝남"이 된다. (왼쪽 메뉴의 루틴 옆 숫자는 늘 이번 달 기준)
- 회차 메모는 **이번 달치만 자동으로** 생긴다. 지난 달은 격자 칸을 누를 때 만들어진다 — 안 한 달이 우르르 살아나 오늘 화면을 덮지 않게.

**주기**는 매월·분기·반기·매년. 해당 없는 달은 칸에 `–`로 비어 있다.

**화면이 잘못돼도 앱은 안 죽는다 (2026-08-11)**: 예전에는 어느 한 곳에 문제가 생기면 **앱 전체가 하얗게** 됐다. 이제는 문제가 난 화면 자리에 **"이 화면에 문제가 생겼습니다"** 안내가 뜨고, 왼쪽 메뉴로 다른 화면에 가면 그대로 쓸 수 있다. 앱 전체가 못 뜨는 경우에도 하얀 화면 대신 안내가 뜨고 **[다시 시도] · [새로고침] · [백업 받기]** 버튼이 있다 — 최악의 경우에도 기록을 파일로 꺼낼 수 있다. 안내에 딸려 나오는 회색 글상자의 내용을 개발자에게 그대로 보여주면 원인을 찾을 수 있다.

## 5-2. 가져오기 — 엑셀 그대로 넣기 · 백업 되돌리기

사이드바 **가져오기**는 두 가지를 받는다.

**⓪ 엑셀에서 복사해 붙여넣기 (회사 PC용, 2026-08-11 추가)** — 루틴 화면 위 **[엑셀에서 붙여넣기]**. 엑셀에서 **머리줄(구분·항목명·담당…)까지 함께 긁어 복사**한 뒤 상자에 붙여넣고 **[읽기] → [N건 넣기]**. 파일을 안 거치므로 **회사 보안 프로그램이 브라우저의 파일 업로드·읽기를 막아도 된다**(실제로 회사 PC에서 파일 가져오기가 막혀 만든 길이다). **숨긴 열은 복사에 안 실려 와도 괜찮다** — 열을 이름으로 찾기 때문에 7~12월만 복사해도 그대로 읽는다. 읽는 규칙은 아래 엑셀 파일과 똑같다.

**① 월간체크리스트 엑셀(.xlsx)** — 정리해 둔 엑셀 파일을 그대로 고르면 앱이 읽어 루틴으로 만든다. 변환 파일을 따로 받을 필요가 없다. (파일이 **엑셀 프로그램에서 열려 있으면 잠겨서 못 읽는다** — 닫고 다시 시도하거나 위 붙여넣기를 쓴다)

- 시트에서 **「항목명」 열이 있는 표**를 스스로 찾는다. 위에 제목·안내 줄이 몇 개 있어도, 열 순서가 바뀌어도 된다.
- `항목명`→이름, `구분`→묶음, `사업장·거래처·기안양식·비고`→**매달 같은 설명** 한 줄.
- `담당`은 **가장 많이 나오는 사람(=나)은 생략**하고 다른 사람일 때만 "담당 ○○"으로 붙는다.
- 1~12월 칸에 표시(O)가 있으면 **그 달은 완료로** 들어간다. 며칠에 했는지는 엑셀에 없으므로 완료일은 비워둔다.
- 넣기 전에 "몇 건을 읽었다"고 확인을 한 번 묻는다.
- **다시 넣어도 안전하다** — 이름이 같은 루틴은 묶음·설명만 갱신되고, **앱에서 손으로 정한 예정일·주기·중단은 그대로** 남는다. 이미 있는 달의 완료 표시도 건드리지 않는다. 즉 엑셀을 고친 뒤 다시 넣으면 그만큼만 따라온다.

**①-2 문서 접수대장 (2026-08-12 추가)** — 「월간체크리스트」 말고 **접수한 문서를 적어둔 표**(접수일·분야·문서명·발행업체·대상·기간/차수·결재·보관위치·메모)도 같은 자리에 붙여넣으면 읽는다. 어느 표인지는 **머리줄을 보고 앱이 알아서 가르므로** 고를 게 없다. 접수대장은 "언제 무엇을 받았다"는 지난 사실이라, 체크리스트와 다르게 들어간다:

- `문서명` → **루틴 이름**, `분야` → 묶음, `보관 위치` → 설명. 없는 이름이면 루틴을 새로 만든다.
- `기간/차수`의 **"6월"이 어느 달 회차인지**를 정한다 (그 칸이 비면 접수일의 달). **12월분을 이듬해 1월에 접수한 것도 12월 회차로** 넣는다.
- **한 달에 여러 차수(6월/1차·2차·3차)면 회차는 하나**, 차수마다 **진행 기록 한 줄**로 쌓인다 — 격자 칸에는 점 하나로 표시되고, 칸을 눌러 열면 1·2·3차가 날짜순으로 보인다.
- 접수됐다 = 그 달 일이 이뤄졌다고 보아 **그 달은 완료로** 표시하고, **완료일에 실제 접수일**을 적는다 (체크리스트는 날짜가 없어 비워두는 것과 다르다). 결재 상태는 기록 줄에 "결재 대기"로 남는다.
- 루틴의 **시작 달은 그 문서의 첫 기록이 있는 달** — 그 앞의 달들은 "해당 없음"이라 안 한 것처럼 빈칸으로 남지 않는다.
- **이미 있는 루틴의 예정일·주기·설명은 건드리지 않는다** (접수대장은 기록이지 규칙이 아니다). **같은 표를 두 번 붙여넣어도 같은 줄은 다시 안 쌓인다.**

**② 백업 파일(.json)** — 백업으로 내려받은 파일을 되돌린다. **없는 것만 추가**되므로 두 번 눌러도 안전하다. 그동안 백업은 내보내기만 있어서 파일을 만들어도 되돌릴 방법이 없었는데, 이제 복원이 된다.

## 6. 메모 — 작업 트래커 (보드 · 달력 · 표 · 타임라인)

노션 작업 트래커처럼 쓴다. 상단 토글로 네 가지 보기를 전환하고, **기본은 보드**. 마지막에 보던 보기를 기억한다 — 단 **폰은 들어올 때 항상 보드부터** 시작한다 (2026-07-19). 달력 보기는 5장 참고.

- **보드** — 할일 · 진행중 · 완료 3열. 카드를 옆 열로 끌면 상태가 바뀌고 (완료 열로 끌면 완료 처리), **같은 열 안에서 끌면 순서가 바뀐다** (저장됨). 폰에서는 카드를 누르면 카드 자리가 상세로 펼쳐진다("접기" 버튼으로 닫기). 기본 정렬은 급한 순 — 밀린 것 → 오늘 → D-n. 카드에 D-day 배지("3일째"·"오늘"·D-n)와 체크 진행률. ~~다음 할 일 힌트 줄~~ — 2026-07-31 제거 (실제로 안 읽게 되어 자리만 차지). **체크가 있는 카드에는 가는 진행 막대 + 숫자(n/m)가 한 줄**로 보인다 — 제목 줄 오른쪽에는 D-배지(공간 좁으면 자동 줄바꿈). **D-n 배지 색 (2026-07-30 단순화)**: 밀림 빨강 · 오늘 주황 · **D-n은 은은한 파랑 하나** — 날짜별 그라데이션은 보드를 알록달록하게 만들어 없앴다 (보드·표 공통). 칸 맨 아래 **"+ 항목 추가"** 로도 그 칸에 새 메모를 만들 수 있다. **진행중 자동 판정은 체크 기준** — 줄만 적어두면 할일에 남고, 체크를 하나라도 하면 진행중으로 넘어간다 (체크 없이 시작한 건 "시작 ›" 버튼이나 드래그로). 완료 열은 **최근 완료한 순**으로 8건만, 나머지는 표에서 (표·타임라인의 완료도 같은 순서).
- **표 (2026-07-30 개편)** — 전체 목록. 상태 / 제목 / 날짜·기간 / 체크 / 작성일, 보관·보류·완료 포함 전부. **컬럼 헤더가 곧 정렬·필터다**: 제목·날짜·체크·작성일 헤더를 누르면 정렬(한 번 ▲, 다시 ▼, 한 번 더 해제), **상태 헤더를 누르면 상태별 걸러 보기**(전체→진행중→할일→보류→보관→완료 순환). 평소엔 상태 그룹 머리줄(▾ 진행중 2)로 묶여 있고 머리줄을 누르면 접힌다. 맨 아래 "+ 항목 추가" 고스트 줄로 새 메모.
- **타임라인** — 월 단위 간트. 상태 규칙과 같은 철학("줄 적음 = 계획, 체크 = 착수")으로 그린다: **할일(착수 전)은 줄이 있어도 현재 예정일 자리에 점만** — 등록일로 소급하지 않고, 하루 미루기/날짜 지정으로 옮기면 점이 따라간다. **진행중(착수 후)은 첫 기록부터 예정일·오늘 중 늦은 쪽까지 막대** — 오래 끄는 건일수록 길어진다. 완료는 활동 구간~완료일, 기간(~) 메모는 기간 그대로. **진행중 → 할일 순으로 묶여 있고 완료는 접혀 있다**("완료 n건 펼치기"). 제목 앞 상태 점(막대와 같은 색), 제목 밑에 구간·체크 요약("7.8 ~ 진행중 · 체크 4/7"). 파란 세로줄 = 오늘. ‹ ›로 달 이동. **막대에 마우스를 올리면 진행기록(최근 8줄·체크 상태)이 카드로 떠서** 클릭 없이 내용을 훑을 수 있다 — 누르면 패널로 전체 보기.
- **검색은 어느 보기에서든 동일** — 제목·진행기록, 보관·완료까지 통합 검색. 보드에서 검색하면 걸린 보관 메모는 아래에 따로 표시되고, 표에서는 일치한 기록 줄이 날짜와 함께 표시.
- **순서도 어느 보기에서든 동일** — 급한 순(밀림 → 오늘 → D-n)이 항상 먼저라 새로 던진 메모는 자기 날짜 자리로 바로 올라온다. 보드에서 드래그한 순서는 **같은 D-day끼리의 순서**만 가르고, 표·타임라인에도 그대로 적용.
- **보류함 (2026-07-28 신설)** — 하다가 멈췄거나 언제 다시 할지 정하지 못한(기약 없는) 일을 넣는 곳. 상세의 **"보류"** 버튼을 누르면 날짜가 떨어지면서 보드·달력에서 사라진다 — 진행기록·작업 설명은 그대로 보존. 사이드바 "보류함 n" 목록에는 줄마다 **보류한 날짜와 마지막 진행기록 한 줄**이 보여서 어디까지 하다 멈췄는지 바로 떠오른다. "오늘로 / 날짜 지정"(개별·일괄)으로 다시 할 일로 꺼내고, 정보 수정으로 날짜를 달아도 보류가 풀린다. 검색에 걸리고 표에서는 청회색 "보류" 배지. **보관함과의 구분**: 보관함 = 자료·메모 넣어두는 곳, 보류함 = 하다 만 일 넣어두는 곳.
- **휴지통** (2026-07-21) — 메모를 삭제하면 바로 사라지지 않고 휴지통으로 간다. **30일 보관 후 자동으로 완전히 삭제**되고, 그 안에는 "복구"를 누르면 원래 자리(보드·달력)로 돌아온다. 버튼에 담긴 개수가 표시된다 (예: "휴지통 2"). 목록 위 체크 줄에서 **전체 선택**을 켜거나 줄을 체크하면 옆에 **"복구 n개" / "완전 삭제 n개"** 버튼이 나온다 — 완전 삭제는 확인을 한 번 묻고, 지우면 되돌릴 수 없다. **완전 삭제해도 다시 살아나던 문제는 2026-08-05 고쳐졌다** — 지운 자리에 "지웠음" 표식만 남겨 다른 기기(집 PC·폰)의 옛 복사본이 도로 올리지 못하게 했다. 표식은 어디에도 보이지 않고 30일 뒤 서버에서도 사라진다.
- **백업 버튼** (PC는 사이드바 맨 아래 로그아웃 옆, 폰은 상단) — 메모·점검·순서 전체를 JSON 파일로 내려받는다. 한 달에 한 번쯤 눌러서 파일을 보관해두면 계정 문제·실수 삭제에 대비한 보험이 된다 (이 파일로 복원 가능)

의도: "25년 ○○업체 계약건 내용 가져와줘" — 몇 년이 지나도 과정 전체가 그대로 나와야 한다. 판 전체를 조망할 땐 보드, 뒤질 땐 표, 일정 감각은 타임라인.

## 7. 점검 탭 (2026-07-14 제거됨)

안전관리 점검 캘린더 탭은 제거되었다. 안전 업무는 엑셀 관리대장(`C:\HDS\안전업무_관리대장.xlsx`)과 앱을 나눠 쓰는 방향으로 재설계 중 — 반복 기한·분야 칩 기능이 들어오면 기한 알림·접수 기록은 앱이 맡는다.

- **데이터는 지워지지 않았다**: 점검 업무·월별 완료·이력·파일은 서버와 백업 JSON에 그대로 있고, 반복 기한 메모로 변환할 예정.
- 점검에서 만들었던 조치·수리 메모는 일반 메모라 그대로 남아 있다.

---

## 로그인과 동기화

- 주소: https://vanguard-cell.github.io/memo-app/ — 회사·폰·집 어디서든 접속.
- 새 기기에서 처음 열면 로그인 화면이 나온다. **"Google로 로그인" 버튼 한 번**이면 끝 — 비밀번호 없음. (구글 계정이 없는 기기는 아래 "이메일 링크로 로그인" 사용)
- 브라우저 설정 등으로 로그인이 풀려도 버튼 한 번만 다시 누르면 된다.
- 어디서 적든 서버에 저장되어 모든 기기에서 같게 보인다. 인터넷이 끊겨도 기기에 저장되고, 화면 위에 "동기화 안 됨"이 뜨면 연결 후 다시 열면 된다.
- **자동 맞춤**: 탭이나 앱으로 다시 돌아오면(화면이 다시 보이면) 서버와 자동으로 맞춘다 — 폰에서 적고 회사 PC 탭으로 돌아오면 잠시 뒤 반영된다. 안 보이면 새로고침 한 번.
- **새 버전 안내 (2026-07-21)**: 데이터와 달리 **앱 코드는 새로고침해야 새 버전**이 된다. 탭을 오래 열어두면 옛 버전이 계속 돌 수 있어서, 새 버전이 배포되면 화면 위에 "새 버전이 배포됐습니다 — 새로고침" 배너가 뜬다. 누르면 끝.

## 알아둘 것
- 상태는 자동: 할일(체크 없음) → 진행중(체크 하나라도 함) → 완료(완료 처리 버튼). 보드에서 드래그·버튼으로 직접 옮길 수도 있다. **상세 액션 줄은 전부 아이콘 한 줄** (2026-07-31 확정): 세로줄 왼쪽 = 자주 쓰는 것(✓완료·→하루 미루기·📅날짜 지정), 오른쪽 = 가끔 쓰는 것(깃발=마감·⏸보류·🗃보관·✎정보 수정·🗑삭제). 마우스를 올리면 설명이 바로 뜬다. 보류·보관·삭제 아이콘은 사이드바의 보류함·보관함·휴지통과 같은 그림 — 누르면 어디로 가는지 그림이 말해준다. (보관 보내기는 던지기 줄 제거 때 사라졌다가 이날 복원)
- **날짜 용어 (2026-08-03 최종 정리 — 어휘 셋만)**: **예정**(호박) = 단일 날짜 + 기간의 시작(시작은 예정에 흡수) / **마감**(빨강) = 기간의 끝 및 "~까지"로 던진 마감형 — 둘을 표기로 구분하지 않는다(깃발 ⚑ 은퇴) / **기간**(보라) = 기간 중간. "시작·종료·만기" 표기는 전부 은퇴. 달력 날짜 목록의 왼쪽 배지는 예정·마감·기간 중·**완료**(회색, 취소선 제목) 중 하나이고, **진행중인 줄은 배지 대신 은은한 초록 바탕**이다. 예정을 진짜 마감으로 바꾸려면 메모 패널의 **"마감으로 지정"** 버튼(반대는 **"마감 해제"**). 버튼은 **하루 미루기**(밀린 건 내일로, 미래 날짜는 하루 뒤로) · **날짜 지정**(고른 날짜로) · **날짜 이동**(달력에서 다른 날로).
- **기간 메모의 D-n은 시작하는 날부터 센다 (2026-08-22 수정)** — 내일 시작해서 3일 뒤 끝나는 교육이 보드에서 **D-3**(끝나는 날)으로 떠서, 급한 순서로도 D-3 무리 사이에 파묻혔다. 이제 **아직 시작 안 한 기간은 시작하는 날까지** 센다(D-1) — 달력에서 기간의 시작을 "예정"으로 보는 것과 같은 규칙이다. **시작한 뒤에는** 끝나는 날을 세면서 앞에 **"마감"** 을 붙인다("마감 D-2") — 안 그러면 어제 D-1이던 게 오늘 D-2로 늘어나 보여 헷갈린다. 보드·표·타임라인의 **급한 순서 정렬도 같은 기준**을 쓴다. "~까지"로 던진 마감형은 예전 그대로(끝나는 날이 전부다).
- **반복 메모 (2026-07-31)**: 정보 수정 → **반복(매주/매달/매년)**. 완료 처리하면 끝나는 대신 **예정일이 다음 주기로 굴러가고 할일로 복귀**한다 — 공과금·정기점검용. 원래 예정일 기준이라 늦게 완료해도 주기가 안 밀리고(지난 주기는 건너뜀), 진행기록은 계속 쌓여 납부 장부가 된다. 카드·표 배지에 **↻**, 상세엔 "↻ 매달" 칩. 완전히 끝내려면 반복을 "없음"으로 바꾸고 완료. (기간 메모에는 적용 안 됨)
- 정보 수정: 메모 패널 → "정보 수정"에서 제목·예정일·기간 수정. **예정일과 기간은 상호 배타** — 한쪽을 입력하면 다른 쪽이 자동으로 비워진다 (기간 값이 남아 있으면 예정일이 조용히 무시되던 버그 방지, 2026-07-21). 기간은 시작 ~ 끝 한 줄.
- 실행 방법: C:\HDS\memo-app 폴더에서 `npm run dev` → 브라우저에서 http://localhost:5173
