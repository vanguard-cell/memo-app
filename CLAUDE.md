# 내 기록 (memo-app)

개인 업무기록 웹앱. 사용자가 회사·집·폰에서 쓰는 1인용 도구다. 상세한 사용법과 각 기능의 의도는 [사용설명서.md](사용설명서.md)를 먼저 읽을 것.

## 최우선 원칙: 심플함

- 새 기능을 넣을 때 "탭·버튼·개념을 늘리지 않고 되는가"를 먼저 물을 것. 추가보다 병합·제거를 우선.
- 화면 구성은 탭 3개(오늘·달력·메모) + 입력창 1개가 전부다. 이 틀을 늘리지 말 것.
- 팝업 금지 — 메모 상세는 오른쪽 사이드 패널. 자동 로그("완료 처리" 같은 것) 금지. 안내 문구 최소화.
- 사용자의 표현: "간단하지만 정확하게!"

## 핵심 컨셉 (사용자 요구사항)

1. 형식 없이 한 줄로 던지면 날짜·기간·업체를 자동 인식하되, 저장 전 칩으로 확인시킨다. 날짜는 제목에서 분리한다 ("7/6 이사" → 제목 "이사", 기한 7/6).
2. 같은 업체의 진행중 메모가 있으면 "이어붙이기"를 먼저 제안한다 — 하나의 사건은 하나의 메모에 (파편화 방지).
3. 기한이 지난 일은 완료할 때까지 오늘 화면에서 계속 괴롭힌다 ("3일째" 식으로). 계약 만기는 D-60부터 미리 알린다.
4. 진행 기록은 누적이며 체크박스로 완료 관리. 달력에는 뜨지 않고 메모 안에서만 산다.
5. 완료된 것도 절대 버리지 않는다 — "25년 ○○업체 계약건" 검색이 몇 년 뒤에도 돼야 한다.
6. 달력에는 모든 메모가 자기 날짜(기한·기간·작성일)에 표시된다. 드래그로 날짜 이동, 날짜별 목록에서 드래그로 우선순위 지정.

## 구조

- Vite + React (JS, TS 아님). 상태는 src/store.js (localStorage 키 `hds-memo-data-v1`, useSyncExternalStore).
- src/parser.js — 한국어 자연어 인식 (날짜·기간·업체). 정규식 기반.
- src/derive.js — 파생 계산 (상태, D-day, 오늘 화면의 괴롭히기 목록).
- src/views/ — TodayView(오늘), CalendarView(달력), MemosView(메모: 목록+검색+업체칩 통합).
- src/components/ — InputBar(던지기+이어붙이기), MemoDetail(사이드 패널), MemoRow.
- 상태는 자동 파생: 할일(기록 없음) → 진행중(기록 있음) → 완료. status 필드는 'open'/'done'만.
- store.js의 migrate()가 로드 시 옛 데이터를 현재 스키마로 변환한다. 스키마를 바꾸면 여기에 마이그레이션을 추가할 것.

## 단계 계획

- 1단계 (완료): 로컬 앱 — 입력·인식·오늘·달력·메모·검색·드래그.
- 2단계 (완료): Supabase 연동 — 기기 간 데이터 동기화.
  - 프로젝트: vfnwworlsricfxyxditc (ap-northeast-2), 연결 정보는 src/supabase.js (anon key는 공개 안전).
  - 테이블: memos(id, user_id, data jsonb, updated_at), app_state(user_id, day_order). 둘 다 RLS로 본인 것만 접근.
  - 로그인: 구글 로그인(기본) + 이메일 매직링크(보조). 사용자 계정: vpfldh87@gmail.com. 구글 OAuth 클라이언트는 사용자의 Google Cloud "My First Project"에 있음.
  - 동기화: 로컬(localStorage) 즉시 반영 + 서버 upsert. 로그인 시 updatedAt 기준 병합. 실패 시 헤더에 "동기화 안 됨" 표시.
  - 배포 주소: https://vanguard-cell.github.io/memo-app/ (main 푸시 시 Actions 자동 배포).
  - 파일 첨부: Storage 버킷 'files' (비공개, 경로 {user_id}/{uuid}.{확장자} — 한글 파일명은 키로 못 쓰므로 uuid 경로 + 원본 이름은 메타데이터에). 메모·점검 공용 FileSection 컴포넌트 + 공용 업로드 헬퍼 src/files.js. files 배열은 data jsonb에 포함되어 함께 동기화.
  - 점검 탭: 파일 드롭 → 새 업무 행 자동 생성(업무명=파일명, 파일 첨부, 수정 폼 자동 오픈). 분야별 정렬 토글. 보고서(src/report.js — 인쇄용 HTML 새 창) + 엑셀(.xlsx, SheetJS) 내보내기.
- 3단계 (예정): PWA 폰 푸시 알림, 필요 시 멀티유저.

## 다음 큰 과제: 보고서 자동화 (2026-07 사용자 확정)

사용자가 원하는 것: /주간보고, /월간보고, /기안서 스킬 — 앱 데이터(점검 이행·조치·특이사항)를 회사 양식에 맞춘 보고서로 자동 작성. [양식/README.md](양식/README.md)에 제작 지침이 있다. 사용자가 양식 폴더에 실제 양식·제출본을 넣으면 그걸 읽고 스킬을 만들 것. **주의: 양식/ 폴더는 gitignore됨(공개 저장소라 회사 문서 커밋 금지)** — 회사·집 PC에 각각 두고 쓴다. 엑셀·회의록·공문 등 일회성 작업은 스킬 없이 대화로 처리하고, 같은 요청이 3번 반복되면 스킬화를 제안할 것. 앱 내 월간보고 버튼(API 연동)은 스킬 검증 후 4단계로.

## UI 재편 방향 (2026-07-22 사용자 확정, 진행 중)

**진행 중인 결정과 확정 사항은 [UI재편-스펙.md](UI재편-스펙.md)에 계속 적는다 — UI 작업 전에 반드시 읽을 것.** (2026-07-24 기준: 던지기 입력 제거·보드 칸별 +로 작성, 오늘/마감·만기 타일 제거, PC 달력 좌우 절반 고정. 재도입 금지 목록은 스펙 참고.)

사용자가 **노션을 모티브로 UI를 재편**하기로 함. 단 "노션을 그대로 베끼지는 않는다" — 가져올 모티브는 **① 구역이 나뉜 화면, ② 항목을 끌어 옮기는 조작, ③ 세부정보의 '작업 설명' 칸**. 목표 인상은 **"웹앱 느낌을 버리고 프로그램(데스크톱 앱) 같은 느낌"**. 상세 패널은 **지금 방식 유지**(본문이 안 밀리고 오른쪽에 뜸 — 달력을 보면서 상세를 볼 수 있어야 함) + 슬라이드 애니메이션. 색조는 이 방향이 정해진 뒤 결정. 폰 화면도 처음부터 같이 설계. 진행 방식: 여기서 텍스트로 디자인 합의 → 최종 프롬프트를 Stitch에 넣어 시안 → 그 시안을 보고 다시 구현 지시.

## 개발 메모

- 실행: `npm run dev` (포트 5173). 빌드: `npm run build`.
- **제거된 기능 — 다시 넣지 말 것**: 음성 입력(마이크 버튼, 2026-07-18 제거 — 브라우저 음성인식이 기기마다 오작동, 특히 아이폰 사파리 미지원). 사용자가 나중에 "회의록 녹음 → 정리" 기능을 원하니 그때 다른 방식(녹음 파일 + AI 정리)으로 설계할 것.
- 폰(<900px) 상세는 누른 줄 **바로 아래** 인라인으로 열려야 한다 (보드·표·타임라인·달력 공통). 목록 맨 밑에 렌더링하면 안 보이는 버그가 된다 (2026-07-18 수정). 폰 달력은 칸에 색 막대만 + **아래에 선택한 날짜 하나의 목록**(오늘 기본 선택, 칸 탭 → 그 자리에서 내용 교체, 스크롤 이동 없음 — 아이폰 달력식). **달 전체를 아래에 나열 + 탭하면 스크롤 점프하는 방식은 사용자가 "조작감 최악"이라고 해서 제거함(2026-07-19) — 재도입 금지.** 좁은 화면 판정은 src/useIsNarrow.js 공용 훅.
- **폰 규칙 (2026-08-05 정리, index.css 맨 끝 `폰 화면 정리` 블록에 모여 있음)**: ① 폰 상단은 PC 사이드바와 같은 아이콘 줄(`.ptab`, 건수 배지 `.ptab-n`, 현재 화면은 `.on`) + 동기화 점(`.sync-dot`, 누르면 runDiagnostics) — 글자 탭(.tabs/.tab)은 삭제됨. ② 누르는 것은 최소 40px, **입력 글씨는 16px 미만 금지**(아이폰이 포커스 때 화면을 확대한다 — textarea·select·date는 `input` 전역 규칙에 안 걸리니 따로 지정할 것). ③ **hover에만 의존하는 UI 금지** — 폰엔 hover가 없다. 아이콘 버튼은 `.pa-tx` 라벨을 같이 넣고(PC는 CSS로 숨김), `.tl-x`처럼 hover로 나타나던 것은 폰에서 상시 표시. ④ 달력 칸에는 글자를 넣지 않는다 — 칩(`.cal-ev`)도 띠(`.cal-band`)도 색 막대로, 공휴일 이름(`.cal-holname`)도 폰에선 숨김(빨간 날짜 숫자로 대신, 이름은 아래 목록 제목에). **칸 머리(`.cal-cell-head`)는 폰에서 높이 20px로 못박혀 있다** — 여기에 한 줄이 늘면 그 칸만 밀려 이어진 띠가 옆 칸과 어긋난다(2026-08-07 공휴일 이름 때문에 실제로 발생). 칸에 새 요소를 추가하면 폰 규칙도 같이 추가할 것. ⑤ `.panel-actions button`(0,1,1)이 `.pa-ic`(0,1,0)를 이기므로 상세 아이콘 크기는 `.panel-actions .pa-ic`로 잡아야 먹는다.
- 폰은 드래그가 안 되므로 인라인 상세(inline prop)에 **상태 버튼 줄(할일/진행중/완료)** 이 있다 — 보드 드래그와 같은 규칙(완료=completeMemo, 그 외=stage 지정+필요시 reopen). PC 패널에는 안 보인다. 폰에서 상태 조작이 안 되는 개편을 하면 안 됨.
- **화면 배율 자동 보정 (2026-07-22)**: 넓은 모니터일수록 글씨·여백이 작아 보이는 문제 때문에, 화면 폭에 따라 `body { zoom }`을 단계로 올린다 (1600px↑ 1.05 / 1900px↑ 1.1 / 2300px↑ 1.15). **배율을 이보다 세게 올리지 말 것** — 1.3~1.45로 넣었더니 "그냥 확대해놓은 것 같고 밸런스가 안 맞는다"는 지적을 받아 낮춤. **zoom은 반드시 body에** — `html`(:root)에 걸면 미디어쿼리 판정 폭까지 줄어들어 배율이 진동한다. vw·vh는 zoom을 안 따라오므로 그걸 쓰는 규칙은 `calc(100vw / var(--zoom) …)`로 보정한다(사이드바 높이, 패널 열렸을 때 본문 max-width, 토스트 폭). 폰(<900px)은 배율 1 그대로.
- 폰 보드는 세로 스택이 아니라 **할일|진행중 2열 나란히**(kb-flat 그리드) + 완료는 접힌 목록. 상세는 grid-column 1/-1로 두 열을 가로질러 전체 폭. 세로로 죽 쌓으면 진행중이 화면 밖으로 밀려 "한눈에 안 보인다"는 지적을 받음 (2026-07-19).
- **화면 오류 안전장치 (2026-08-11)**: `src/components/ErrorBoundary.jsx`를 두 겹으로 쓴다 — main.jsx가 앱 전체를(`whole`, 백업 받기 버튼 포함), App.jsx가 화면마다(`key={screen}`이라 화면을 옮기면 오류가 저절로 풀림). 루틴을 넣다가 무한 렌더로 **로그인 화면조차 못 그리고 하얗게 남은 사고**가 계기. ⚠️ 리액트 안전장치는 **그리는 중의 오류만** 잡는다(이벤트 핸들러 오류는 못 잡음). 그리고 **`useSyncExternalStore`의 getter에서 filter·sort로 새 배열을 만들지 말 것** — 그게 그 사고의 원인이었다. 화면이 읽는 목록은 `withVisible()`에서 한 번 만들어 담아둔다(`visible`·`trash`·`routineList`).
- **루틴 (2026-08-11 신설)**: 사이드바 두 번째 항목. 매달·분기·해마다 도는 일을 **항목×12개월 격자**로 본다 (엑셀 「월간체크리스트」를 옮긴 것). ⭐ **정의와 회차를 나눈 게 핵심** — 정의(`state.routines`, 서버엔 `type:'routine'` 행)는 반복 규칙(이름·묶음·매달 같은 설명·예정일·해당 월)이고, 회차는 **그냥 메모**(`routineId`+`ym`)라서 달력·오늘·검색·진행기록·파일이 공짜로 붙는다. 그 달만의 특이사항은 회차 상세에 쌓이고 격자엔 점으로만 표시. **`repeat`(메모 하나가 굴러가는 방식)를 회차에 쓰지 말 것 — "몇 월에 했나"가 안 남는다.** 회차는 미리 12개를 만들지 않는다(이번 달치만 앱 열 때 batch로). 항목 빼기는 **삭제가 아니라 중단(`endYm`)** — 지난 기록은 남는다. 2026-07-14에 점검 탭을 뺀 이유가 "엑셀과 이중 입력"이었으므로, **루틴은 새 데이터를 만들지 않고 메모를 격자로 보여주기만 한다**는 선을 지킬 것.
- **지운 회차는 다시 안 만든다 (2026-08-19)**: `ensureThisMonth`의 "이미 있나" 판정은 `routineCycle`(=`state.visible`, 지운 건 빠짐)이 아니라 **`cycleEverMade`(`state.memos`, 휴지통 표식까지 포함)**로 한다 — visible로 보면 내가 지운 회차를 앱이 다음에 열 때 다시 만들어낸다. 이름 판정도 `trim()`이 아니라 공용 `blankTitle()`(제로폭 공백 등 안 보이는 글자까지 빈 것으로) — 엑셀에서 딸려온 보이지 않는 글자 때문에 "— 8월분" 회차가 계속 생겼다. 새 판정을 쓰는 곳: store(ensureThisMonth·cleanupBlankRoutines) · RoutineView(건수·열기) · CalendarView(예정 자리) · App(사이드바 배지).
- **앞으로 올 달의 루틴 "예정 자리" (2026-08-13)**: 달력이 다음 달 이후를 볼 때 루틴 정의를 읽어 칩을 그린다(`ghosts` in CalendarView) — **회차 메모는 안 만든다**. 누르면 `ensureCycle`로 그때 만들어진다. 미리 만들면 안 한 달이 오늘 화면·보드에 쌓이므로 그 선(이번 달만 `ensureThisMonth`)은 그대로 두고 달력에서만 보여주는 것. 검색 중(`filtered`)엔 안 그린다(있는 것만 보는 자리 + 필터된 memos로 중복 판정이 어긋난다). 칩은 `.cal-chips` **안에** 넣을 것 — 밖에 두면 flex:1에 밀려 칸 아래에 붙는다.
- **가예정 (2026-08-13, 사용자: "루틴이 자동으로 잡은 날인지 내가 업체와 잡은 날인지 구분이 안 된다")**: 루틴 정의에 `flexible`(고정↔매번 잡음), 회차 메모에 `tentative`. `flexible` 루틴의 회차는 `tentative:true`로 태어나 달력·목록·보드에서 흐리게(PC 칩은 점선 + 속 비움, **폰 칩은 글자색이 투명이라 currentColor 점선이 안 보인다 → 흐리기만**). 푸는 곳은 세 군데뿐 — `updateMemo`의 `settle()`(due·period가 패치에 있으면 해제), `addHistory`, `completeMemo`. **`setGroupDueDay`는 updateMemo를 안 거치므로 안 풀린다 — 그게 맞다**(앱이 옮긴 날짜는 내가 잡은 날이 아니다). 옛 데이터는 두 필드가 없으므로 자동으로 '고정·확정' — 마이그레이션 불필요.
- **엑셀 표 읽기 (2026-08-12)**: `importXlsx.js`가 표 두 종류를 **머리줄로 알아서 가른다** — '항목명'이면 월간체크리스트(`readRoutineGrid`→`importRoutineRows`), '문서명'이면 문서 접수대장(`readReceiptGrid`→`importRoutineCycles`). 붙여넣기·파일 둘 다 같은 갈림길을 탄다. 열 이름은 `norm()`으로 공백·괄호·가운뎃점·빗금을 떼고 견주므로 "발행 업체"/"발행업체"가 같이 걸린다. **접수대장은 기록이지 규칙이 아니다** — 회차를 완료로 넣되(완료일=실제 접수일) **기존 루틴 정의는 안 건드린다**. 한 달에 여러 차수는 회차 하나 + 진행기록 여러 줄(`type:'log'`)로 — 회차는 (루틴, 연월) 하나뿐이라는 모델을 깨지 말 것. 새 루틴의 `startYm`은 **그 문서의 첫 기록 달**(전체 최소를 쓰면 늦게 시작한 항목이 앞 달에 '안 한 칸'으로 남는다). 같은 표를 다시 붙여넣어도 날짜+글이 같은 줄은 안 쌓인다.
- **가져오기 (2026-08-11)**: 사이드바 백업 옆. **.json**(백업 되돌리기, `importData` — 같은 id는 건드리지 않음)과 **.xlsx**(월간체크리스트 → 루틴, `src/importXlsx.js` + `importRoutineRows`) 둘 다 받는다. 엑셀은 '항목명' 열이 있는 표를 스스로 찾고, 이름이 같은 루틴은 **묶음·설명만 갱신**한다(예정일·주기·중단은 손대지 않음 — 다시 넣어도 앱에서 손으로 정한 게 안 날아가게). ⚠️ **exceljs는 1MB에 가까워 반드시 동적 import로 둘 것** — 지금은 별도 청크라 엑셀을 고른 순간에만 받아온다(메인 번들 495KB 유지).
- **글씨·입력칸 규칙 (2026-08-11, 사용자: "글씨 크기에 통일성이 없다 / 여기 써야겠군 하는 느낌이 안 든다")**: 글씨 크기는 index.css 맨 위 `--fs-title(15) / --fs-base(12.5) / --fs-sm(11.5) / --fs-label(11)` **네 단계만** 쓴다 — 새 규칙에 px를 직접 적지 말 것(칩·아이콘 등 10.5px 이하 예외만 남아 있음). **쓰는 칸은 전부 같은 얼굴**: 평소 `--field` 바탕 + 1px 테두리, 포커스 때 흰 바탕 + 파란 테두리 + 옅은 링. 공통 규칙 묶음(`.panel-title-input, .desc-input, .tl-input, .tl-date-input, .meta-date-input`)에 새 입력칸 이름을 추가하면 된다. 칸 이름표는 `.panel-sec-label`(= `.tl-title`) 하나로 통일. 글꼴은 Pretendard(index.html에서 jsDelivr CDN, 막히면 기존 시스템 글꼴로 자동 폴백).
- 사용자는 개발자가 아니다. 설명은 쉬운 한국어로, 기술 용어 최소화.
- 기능을 바꾸면 사용설명서.md도 같이 갱신할 것.
